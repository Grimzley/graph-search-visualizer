{
  "name": "graph-search-visualizer",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf public",
    "copy:src": "mkdir -p public && copyfiles -f src/index.html src/app.py public && copyfiles -u 1 src/**/* public",
    "copy:bootstrap:css": "copyfiles -f node_modules/bootstrap/dist/css/bootstrap.min.css public/css",
    "copy:bootstrap:js": "copyfiles -f node_modules/bootstrap/dist/js/bootstrap.bundle.min.js public/js",
    "copy:bootstrap": "npm run copy:bootstrap:css && npm run copy:bootstrap:js",
    "build": "npm run clean && npm run copy:src && npm run copy:bootstrap",
    "dev": "chokidar \"src/**/*\" -c \"npm run copy:src\""
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.0.1"
  }
}
